// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Users {
  uid           Int      @id @default(autoincrement())
  username      String   @unique
  hash          String?
  avatar        String?  
  intra_id      Int      @unique
  dblauth       Boolean  @default(false)
  first_login   Boolean  @default(true)
  mail          String?  @unique
  friends       String[]
  blacklist     String[]
  groups        String[]
  games         GameOnUsers[]
}

model Session {
  id        String   @id
  sid       String   @unique
  data      String
  expiresAt   DateTime
}

model Profils {
    uid     Int @unique @default(autoincrement())
    image_url   String @unique
    hash        String?
    type        String?
    information String      @id @default(uuid())
}

model Game {
    uid         Int     @unique @default(autoincrement())
    score       String
    isActive    Boolean @default(false)
    users       GameOnUsers[]
}

model GameOnUsers {
    player      Users   @relation(fields: [playerId], references: [uid])
    playerId    Int
    game        Game    @relation(fields: [gameId], references: [uid])
    gameId      Int

    @@id([playerId, gameId])
}
